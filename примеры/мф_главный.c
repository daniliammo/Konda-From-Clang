// Главный файл многофайлового примера: «&с» уходит в функции из мф_счёт.c.
#include <stdio.h>
#include "мф_счёт.h"

int main(void) {
    Счёт с;
    счёт_обнулить(&с);
    счёт_прибавить(&с, 7);
    счёт_прибавить(&с, 5);
    printf("значение=%d шагов=%d\n", с.значение, с.шагов);
    return 0;
}
