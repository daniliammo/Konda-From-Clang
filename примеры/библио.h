/* «Библиотека»: out-параметры через указатель — классический C-API.
   kfc сгенерирует «внешняя … (изменяемый …)» и включит этот заголовок. */
#pragma once
#include <stdint.h>
static inline void сдвинуть_на(int32_t *значение, int32_t на) { *значение += на; }
static inline void обнулить_знач(int32_t *значение) { *значение = 0; }
