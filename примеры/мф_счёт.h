// Заголовок проекта для многофайлового примера: структура живёт в .h,
// как в настоящем C-проекте. kfc берёт типы из НЕсистемных заголовков
// и эмитит каждый ровно один раз на проект.
#ifndef МФ_СЧЁТ_H
#define МФ_СЧЁТ_H

typedef struct {
    int значение;
    int шагов;
} Счёт;

void счёт_обнулить(Счёт *с);
void счёт_прибавить(Счёт *с, int д);

#endif
